logging:
  level: 
    com:
      sivalabs: debug

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/catalog?useSSL=false
    driverClassName: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
